<% if model.errors.any? %>
  <div class="alert">
    <div class="list-unstyled">
        <h5><%= model.errors.count%>件のエラー</h5>
        <%= model.errors.full_messages.each do |message| %>
            <li><%= message%></li>
        <% end %>
    </div>
  </div>
<% end %>