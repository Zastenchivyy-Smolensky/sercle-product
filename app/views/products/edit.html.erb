<section class="hero is-success">
  <div class="hero-body">
    <div class="container">
      <h1 class="title">
        レシピ編集
      </h1>
    </div>
  </div>
</section>

<% if @product.errors.any? %>
  <div class="notification is-danger">
    <h2><%= pluralize(@product.errors.count, "error") %> prohibited this object from being saved: not successfully</h2>
    <ul>
      <% @product.errors.full_messages.each do |message| %>
      <li><%= message %></li>
      <% end %>
    </ul>
  </div>
<% end %>

<section class="section">
    <div class="container">
        <div class="columns is-centered">
            <div class="column is-6">
                 <%= form_for @product do |f| %>
                 <div class="field">
                    <%= f.label :title,"アプリのタイトル", class:"label"%>
                    <%= f.text_field :title, class:"input" %>
                 </div>
                 <div class="field">
                    <%= f.label :content,"アプリの説明", class:"label" %>
                    <%= f.text_area :content, class:"textarea" %>
                 </div>
                 <div class="field">
                    <%= f.label :tech,"技術", class:"label" %>
                    <%= f.text_area :tech, class:"textarea" %>
                 </div>
                 <div class="field">
                    <%= f.label :span,"制作期間", class:"label" %>
                    <%= f.text_area :span, class:"number" %>
                 </div>

                 <div class="field">
                    <%= f.label :image,"アプリの写真", class:"label" %>
                    <%= f.attachment_field :image, class:"input" %>
                 </div>

                 <div class="field">
                    <%= f.label :github,"GitHubのリンク", class:"label" %>
                    <%= f.text_area :github, class:"textarea" %>
                 </div>

                 <div class="field">
                     <%= f.label :link,"GitHubのリンク", class:"label" %>
                    <%= f.text_area :link, class:"textarea" %>
                 </div>

                 <div class="field">
                    <%= f.label :commitment,"こだわり", class:"label" %>
                    <%= f.text_area :commitment, class:"textarea" %>
                 </div>
                <%= f.submit "投稿", class:"button is-success" %>
                <% end %>
            </div>
        </div>
    </div>
</section>


                   

                    

                    

                    
               